name: Build Dex

on:
  push:
    branches: [ "master", "main" ]
    tags: [ "v*" ]
  pull_request:
    branches: [ "master", "main" ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x ./gradlew

    - name: Build All
      run: ./gradlew buildAll

    - name: Copy artifacts
      run: |
        cp build/dex/classes.dex .
        cp build/plugin/loader.elyx .

    - name: Upload Dev artifacts
      uses: actions/upload-artifact@v4
      with:
        name: re-extera-dev
        path: |
          classes.dex
          loader.elyx

    - name: Parse tag for release name
      id: parse_tag
      if: startsWith(github.ref, 'refs/tags/v')
      run: |
        TAG_NAME=${GITHUB_REF#refs/tags/}
        WITHOUT_V=${TAG_NAME#v}
        PLUGIN_VER=${WITHOUT_V%-*}
        TG_VER=${WITHOUT_V#*-}
        echo "plugin=$PLUGIN_VER" >> $GITHUB_OUTPUT
        echo "tg=$TG_VER" >> $GITHUB_OUTPUT

    - name: Upload to Release
      if: startsWith(github.ref, 'refs/tags/v')
      uses: softprops/action-gh-release@v2
      with:
        name: v${{ steps.parse_tag.outputs.plugin }} for ${{ steps.parse_tag.outputs.tg }}
        files: |
          classes.dex
          loader.elyx
        generate_release_notes: true
